services:
  - type: web_service
    name: duckduckgo-query-scraper
    env: python
    plan: free
    branch: main
    region: oregon
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn app:app --host 0.0.0.0 --port $PORT"
    healthCheckPath: "/"
